{
  "name": "four-sigma-dashboard",
  "private": true,
  "scripts": {
    "dev": "cd .. && py -3 -m web.server",
    "snapshot": "cd .. && py -3 -m web.export_dashboard",
    "deploy": "npm run snapshot && npx vercel",
    "deploy:prod": "npm run snapshot && npx vercel --prod"
  }
}
