do not hallucinate any data points. make sure theyre all from accurate sources 

the deterministic parts must remain deterministic, do not make it a wrapper. 

do not take shortcuts or bypasses, use the full pipeline when evaluating. 

do not create bloated code. use the folder structure already created. 
